#include <string>
#include <math.h>
using namespace std;

class Reverse
{
public:
    int reverseDigit(int v);
    string reverseString(string str);
    void reserveStringUtil(string str, int pos, char ps[]);
};